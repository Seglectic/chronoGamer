[project]
name = "chronoGamer"
version = "0.1.0"
description = "Retro game chronological timeline browser (pre-PS2 era, US releases)."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "pyinstaller>=6.16.0",
]

[tool.uv]
package = false
